# ZeroKey for Android

The Android client is not started.

This folder is reserved for it. When it is built, it must generate the same passwords as the web app in `Web/`. Do not change the PBKDF2 or memorable algorithms in `Web/app/lib/generators/`. Existing passwords depend on those functions staying exactly as they are.
